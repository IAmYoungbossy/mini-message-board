extends layout

block content
  h1= title
  p Welcome to #{title}

  div
    each message in mssgArr
      div
        h2 #{message.text}
        p #{message.user}
        p #{message.added}
        
  a(href="/new") Add New Message